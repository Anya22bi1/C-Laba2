//Дано N чисел, требуется выяснить, сколько среди них различных.
//
//Входные данные
//В первой строке дано число N – количество чисел. (1 <= N <= 100000) Во второй строке даны через пробел N чисел, каждое не превышает 2 * 109 по модулю.
//
//Выходные данные
//Выведите число, равное количеству различных чисел среди данных.

#include <iostream>
#include <set>

int main() {
    int N;
    std::cin >> N; //считываем кол-во чисел

    std::set<int> numbers; //создаем сет для работы с числами (особенность сета в том, что в нем не повторяются числа)
    for (int i = 0; i < N; ++i) {
        int num;
        std::cin >> num; //считываем вводимые числа
        numbers.insert(num); //добавляем их рандомно в сет
    }

    std::cout << numbers.size() << std::endl; //выводим размер сета (тк он не хранит одинаковые числа, его размер будет равен числу различных чисел)

    return 0;
}
